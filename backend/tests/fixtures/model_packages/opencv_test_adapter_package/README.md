# OpenCV Test Adapter Package Fixture

This fixture exists only to validate the model handoff, import, validation,
activation, dry-run test and report export flow.

It is not a production YOLO model and must not be used as a competition
detector. All outputs are advisory metadata only and no physical command is
generated.
