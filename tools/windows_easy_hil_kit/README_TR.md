# ISTIKLAL Windows Kolay HIL Paketi

Ana rehber: `OKU_BENI.html`. ZIP'i klasöre çıkardıktan sonra `00_BASLA_BURADAN.cmd` ile açın.

Paket özel anahtar, parola, Tailscale tokeni, YOLO `.pt` modeli, `.venv`, `node_modules` veya eski release içermez.
